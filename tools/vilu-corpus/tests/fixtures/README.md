# Synthetic fixtures only

Most tests generate bounded synthetic JSONL records in temporary directories.
`common-corpus-ff9892ec-representative.txt` is a checksum-pinned synthetic
regression sample shaped after the observed filter/relevance outcomes of the
approved Common Corpus revision. It contains no upstream Common Corpus full
text, personal data, or production data.
