import{describe,expect,it,vi}from'vitest';const mocks=vi.hoisted(()=>({rpc:vi.fn()}));vi.mock('../../../lib/supabase',()=>({supabase:{rpc:mocks.rpc}}));import{getEmployerOutcomes}from'../api';import{employerCopy}from'../copy';
describe('employer outcomes boundary',()=>{it('binds the latest completed UTC month to the active organization',async()=>{mocks.rpc.mockResolvedValue({data:{cohortSize:20},error:null});await getEmployerOutcomes('org-a',0);const args=mocks.rpc.mock.calls[0][1];expect(mocks.rpc).toHaveBeenCalledWith('get_employer_outcomes',expect.objectContaining({target_organization_id:'org-a'}));expect(new Date(args.target_from).getUTCDate()).toBe(1);expect(new Date(args.target_to).getUTCDate()).toBe(1);expect(new Date(args.target_to).getTime()-new Date(args.target_from).getTime()).toBeGreaterThanOrEqual(28*86400000)});it('keeps RU and EN complete',()=>{expect(Object.keys(employerCopy.ru)).toEqual(Object.keys(employerCopy.en));expect(employerCopy.ru.privacyBody).toMatch(/[А-Яа-я]/)})});
