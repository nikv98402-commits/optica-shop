# Project TODOs

## Design system

- [ ] Update `DESIGN.md` to make ViLu lime (`#d8ef4f`) the primary action color and
  remove the stale amber (`#f5b25f`) guidance. Audit shared controls afterward so
  dark surfaces use paper/white text, light surfaces use ink text, and primary
  actions remain consistent across checkout, try-on, catalog, and payment results.
  This is intentionally deferred from the service-checkout plan because it is a
  product-wide design-system migration rather than a checkout-only change.
